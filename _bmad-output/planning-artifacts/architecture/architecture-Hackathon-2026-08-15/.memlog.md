---
scope: Electron hackathon reconciliation prototype
purpose: Lean build substrate for implementation agents
altitude: feature
updated: 2026-08-15T13:56
---

- (direction) Fast path; build substrate only; audience is the hackathon implementation team
- (constraint) Approved PRD FR-1..FR-19 and NFR-1..NFR-7 bind the spine; DESIGN.md and EXPERIENCE.md own visual and interaction detail
- (decision) AD-1 binds all: use a process-isolated modular monolith so renderer, preload, main application, domain, and adapters cannot collapse into incompatible responsibility models
- (decision) AD-2 binds all modules: dependencies point renderer -> typed preload API -> main application services -> pure domain -> repository/report adapters; domain imports no Electron, React, SQLite, filesystem, or Excel code
- (decision) AD-3 binds persisted data: Electron main process is the sole owner of SQLite and filesystem state; renderer owns only ephemeral view state and never receives database or filesystem primitives
- (decision) AD-4 binds IPC: one shared Zod schema defines each command/query input and result envelope; preload exposes an explicit allowlisted API; main revalidates input and sender before dispatch
- (decision) AD-5 binds reconciliation: implement matching as a pure deterministic domain function using the PRD key, stable Trade-ID ordinal pairing, and canonical Reconciliation Result; no database calls inside the engine
- (decision) AD-6 binds mutations: all writes flow through main application commands and a transaction; queries return immutable DTO snapshots; successful mutations return changed DTOs and renderer updates from that response
- (decision) AD-7 binds run lifecycle: main enforces at most one active reconciliation command; each accepted command creates a new Run ID and commits run, source trades, results, and metrics atomically
- (decision) AD-8 binds report output: generate to a sibling temporary XLSX, reopen and validate required sheets, then atomically rename to the collision-safe final filename; failure removes only the temp file and preserves the run
- (decision) AD-9 binds security: renderer sandbox and context isolation stay enabled; nodeIntegration stays disabled; packaged local content only, restrictive CSP, blocked navigation/new windows, no raw ipcRenderer exposure
- (decision) AD-10 binds errors and progress: every IPC response uses success(data) or failure(code,message,retryable,field?) and progress events carry runId, phase, completed, total; user-entered state remains in renderer until success
- (decision) AD-11 binds UI consistency: features use shared design tokens and semantic primitives; Results renders a native semantic table driven by stable TanStack Table v8, with no virtualized DOM for the 1,000-result prototype fixture
- (decision) AD-12 binds testability: clock, ID generator, fixture registry, database path, and report directory enter application services as ports; unit tests never depend on wall clock, userData, or OS dialogs
- (decision) AD-13 binds environment and delivery: one packaged desktop app and one local SQLite file under Electron userData; seed once through versioned migrations; local mock-output path is config, not hard-coded business logic
- (decision) AD-14 binds feature boundaries: each main module owns its commands, queries, repository interface, and mapping; modules may share domain value objects and contracts but may not reach into another module's adapter
- (version) Electron 43.2.0 with bundled Node 24.18.0; verified against the Electron stable releases page
- (version) Electron Forge 7.11.2 with official Vite-TypeScript template and Vite 8.0.16; Vite plugin is experimental, so exact versions and package-lock are committed
- (version) React 19.2.7; verified on react.dev versions
- (version) TypeScript 7.0.x and Zod 4.4.3; strict mode; verified against official release sources
- (version) TanStack React Table 8.21.4 stable; v9 is beta and excluded
- (version) Node built-in node:sqlite from Electron's Node 24.18 line; release-candidate API accepted for prototype behind a repository adapter to avoid native addon packaging
- (version) ExcelJS 4.4.0 for XLSX generation and validation
- (version) Vitest 4.1.x for unit/component tests and Playwright 1.60.x Electron automation for critical end-to-end flows
- (assumption) npm is the package manager and package-lock.json is authoritative
- (assumption) macOS is the hackathon packaging target; architecture keeps OS-specific paths behind Electron APIs
- (direction) No extra architecture deck, C4 package, or broad solution-design document is produced unless requested
- (decision) AD-15 binds renderer state: persisted DTOs are loaded by view-level hooks and replaced from command/query responses; ephemeral filter, sort, selection, inspector, and draft-comment state stays in the owning feature; do not add Redux, Zustand, or a second persistence cache
- (decision) AD-16 binds presentation: one CSS custom-property token file implements DESIGN.md and system themes; feature styling uses CSS Modules; do not adopt a component skin or utility framework that creates a second token system
- (decision) AD-17 binds expensive work: XLSX generation and reopen-validation execute in a Node worker thread using a plain RunReport DTO; worker receives no database handle and returns only a report receipt or typed failure
- (decision) AD-18 binds schema evolution: SQLite schema changes are ordered, transactional migrations keyed by PRAGMA user_version; seed fixtures import only after migrations and only when seed version is absent
- (decision) AD-19 binds navigation: renderer uses a closed AppView union for Overview, Runs, and Exceptions plus selected Run ID; no router library or URL-derived state in the prototype
- (decision) AD-20 binds financial comparison: amount, quantity, and price cross boundaries and persist as normalized decimal strings; domain comparison and sorting use shared decimal-string helpers, never binary floating-point equality
- (constraint) PRD is authoritative over stale EXPERIENCE wording: mandatory review and the report gate apply to unmatched Results only; EXPERIENCE.md was aligned
- (decision) AD-21 binds workflow eligibility: main reloads authoritative persisted state and enforces reviewRequired(status)=unmatched, commentAllowed(status)=status!=matched, broker preview includes only the chosen broker's unmatched Results, and report save requires every reviewRequired Result reviewed
- (decision) AD-22 binds anomaly provenance: baseline is the arithmetic mean of exactly five seeded historical unresolved rates, excludes user runs, returns insufficient-history below five, and reads the dual threshold from typed config
- (decision) AD-23 binds workspace composition: demo width keeps semantic table and persistent adjacent Detail Panel together; constrained widths replace only the panel with an explicit inspector and never squeeze required columns
- (decision) AD-24 binds accessibility modes: tokens follow active OS light/dark, forced-colors uses system colors, reduced-motion removes nonessential motion, and contrast meets the PRD floors
- (decision) AD-25 binds focus and announcements: row selection retains table focus; explicit inspector open moves to heading and close restores invoker; progress/success are polite live updates and errors are assertive and control-associated
- (decision) AD-26 binds in-place recovery: navigation, theme, filtering, selection, and detail updates never reload renderer; retry preserves selected Run, Result, focus context, and drafts; report success returns destination
- (change) Clarified AD-3 filesystem ownership includes main-owned report worker; worker has only the one output capability supplied for its operation
- (decision) AD-27 binds aggregate ownership: Runs module alone owns RunAggregate persistence and the cross-feature Unit of Work
- (decision) AD-28 binds report snapshot: main evaluates eligibility and builds RunReportV1 in one read transaction; worker output uses that immutable snapshot
- (decision) AD-29 binds identity and retries: ASCII Trade IDs compare bytewise, duplicate IDs fail, and mutation commands set values idempotently
- (decision) AD-30 binds demo reset: documented app-closed npm script reuses bootstrap migrations and seeding and never deletes reports
- (version) Refreshed current pins after reviewer verification: Electron 43.4.0/Node 24.18.1, Vite 8.2.1, TypeScript 7.0.2, TanStack React Table 9.1.2, Vitest 4.1.7, Playwright 1.62.1
- (change) Report worker contract is Zod-versioned; worker validates temp workbook; main owns final atomic rename and receipt
- (version) Final patch refresh: React 19.2.8 and Vitest 4.1.10
- (event) Input reconciliation complete; PRD and UX contracts landed, including canonical unmatched-only review gate
- (event) Reviewer gate passed after fixes: lint clean and no critical/high rubric, current-tech, or adversarial findings remain
- (event) Structure and prose polish complete without changing AD IDs or decisions
- (event) spine finalized
